-- script

local gameState = {}

-- input.assign(input.A, 4)

-- scene.clear()

-- i = scene.image("kagamin.jpg", 0)
ddd = scene.dialog({"hello lol", "", "", ""}, 4)
print( debug.getmetatable(scene.dialog).__call )
print(ddd)

-- script.waitfor(ddd)
